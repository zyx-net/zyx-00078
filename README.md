# 社区团购售后仲裁系统

面向团长、商家、客服三类角色的售后仲裁管理系统，支持售后申请登记、状态受控流转、版本审计追踪和数据持久化。

---

## 一、项目结构

```
zyx-00078/
├── api/                     # 后端Express服务
│   ├── db/                  # 数据库初始化
│   ├── repositories/        # 数据访问层
│   ├── services/            # 业务逻辑层
│   ├── middleware/          # 中间件（认证、权限）
│   ├── routes/              # API路由
│   ├── app.ts               # 应用入口
│   └── server.ts            # 服务器启动
├── src/                     # 前端React应用
│   ├── pages/               # 页面组件
│   ├── components/          # 通用组件
│   ├── store/               # 状态管理
│   └── utils/               # 工具函数
├── shared/                  # 共享类型定义
├── data/                    # SQLite数据库文件（运行后生成）
└── README.md
```

---

## 二、启动命令

### 前置要求
- Node.js >= 18
- npm >= 9

### 安装依赖
```bash
npm install
```

### 启动服务

**方式一：同时启动前后端（推荐）**
```bash
npm run dev
```
- 前端：http://localhost:5173（若被占用则自动使用5174等端口）
- 后端：http://localhost:3001

**方式二：分别启动**
```bash
# 启动后端（端口3001）
npm run server:dev

# 启动前端（端口5173，另开终端）
npm run client:dev
```

### 类型检查
```bash
npm run check
```

---

## 三、演示账号

系统预置3个角色账号，密码均为 `123456`：

| 角色 | 用户名 | 姓名 | 说明 |
|------|--------|------|------|
| 团长 | `leader1` | 李团长 | 发起售后申请、补充凭证 |
| 商家 | `merchant1` | 张商家 | 响应售后、上传商家凭证 |
| 客服 | `cs1` | 王客服 | 仲裁裁决、导出退款清单 |

**登录地址**：http://localhost:5173/login

### 账号登录验证
首次启动服务时，系统会自动初始化演示账号。如果遇到登录失败（401错误），系统会自动修复密码哈希兼容性问题。

**验证登录接口（可选）**：
```bash
curl -X POST http://localhost:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"cs1","password":"123456"}'
```

**预期返回**：
```json
{
  "success": true,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 3,
      "username": "cs1",
      "name": "王客服",
      "role": "cs",
      "createdAt": "2026-06-06T07:00:00.000Z"
    }
  }
}
```

---

## 四、核心功能

### 1. 三类角色
- **团长**：发起售后申请（缺货/破损/错发）、补充凭证
- **商家**：查看售后、响应处理、上传商家凭证
- **客服**：仲裁裁决（同意退款/驳回）、导出退款清单

### 2. 售后类型队列
- 缺货（outOfStock）
- 破损（damaged）
- 错发（wrongDelivery）

### 3. 状态流转（受控状态机）
```
待举证(pendingEvidence)
    ↓ 团长提交凭证
商家处理(merchantProcessing)
    ↓ 商家响应
客服仲裁(csArbitration)
    ↓ 客服裁决           ↓ 客服驳回
退款完成(refundCompleted)  驳回(rejected)
```

### 4. 版本控制
- 每步操作递增版本号
- 使用乐观锁防止旧版本重复处理
- 完整审计历史：操作人、备注、时间、版本号

### 5. 筛选查询
- 按售后类型筛选
- 按状态筛选
- 按责任方筛选
- 关键词搜索

### 6. 数据持久化
- 使用SQLite本地文件存储
- 重启后队列顺序、审计历史、案件状态、导出内容完全一致

---

## 五、正常链路验证（破损退款）

### 步骤1：团长发起破损售后申请
1. 使用账号 `leader1` / `123456` 登录
2. 点击「新建售后申请」
3. 填写表单：
   - 订单号：`DD202601001`
   - 售后类型：**破损**
   - 商品名称：`进口牛奶 1L装`
   - 数量：`2`
   - 退款金额：`58.00`
   - 责任方：`商家`
   - 商家：`张商家`
   - 问题描述：`收到时包装破损，液体渗漏`
4. 提交后，案件进入「待举证」状态

### 步骤2：团长补充凭证
1. 在案件列表中点击刚创建的案件进入详情
2. 在「提交凭证」区域填写：
   - 凭证类型：`图片`
   - 凭证URL：`https://example.com/damaged1.jpg`
   - 备注：`破损照片1`
3. 提交后，状态流转为「商家处理」

### 步骤3：商家响应
1. 退出登录，使用账号 `merchant1` / `123456` 登录
2. 进入案件详情，在「商家响应」区域填写：
   - 凭证URL：`https://example.com/merchant-evidence.jpg`
   - 备注：`确认是运输过程中破损，同意退款`
3. 提交后，状态流转为「客服仲裁」

### 步骤4：客服裁决
1. 退出登录，使用账号 `cs1` / `123456` 登录
2. 进入案件详情，点击「同意退款」按钮
3. 填写备注：`情况属实，同意全额退款`
4. 提交后，状态流转为「退款完成」

### 步骤5：导出退款清单
1. 客服账号登录状态下，点击左侧「退款导出」
2. 选择日期范围（包含当前日期），点击「查询」
3. 可在列表中看到刚完成的退款案件
4. 点击「导出CSV」，下载退款清单文件

---

## 六、批量退款确认功能

### 功能概述
客服可以从已进入「客服仲裁」状态的案件中勾选多笔，批量同意退款或批量驳回。系统提供乐观锁校验、部分失败处理、审计追踪和CSV导出能力。商家和团长无权限使用此功能。

### 核心特性
- **权限控制**：仅客服（cs角色）可见批量操作入口和调用API
- **预览校验**：提交前展示每笔当前状态、版本号、退款金额汇总和不可处理原因
- **乐观锁**：逐笔使用版本号校验，已被他人处理的案件不会被误改
- **部分失败**：返回详细的成功/失败/跳过明细，不影响其他正常案件
- **审计追踪**：每笔批量操作生成批次号，完整记录操作人、备注、每笔结果
- **CSV导出**：支持按批次导出操作结果
- **数据持久化**：重启后批次记录、案件状态、审计和导出内容完全一致

---

### 验收步骤

#### 链路1：成功批量退款（全部可处理）

**操作步骤**：
1. 登录客服账号 `cs1` / `123456`
2. 进入「案件列表」，确保至少有3笔状态为「客服仲裁」的案件
3. 勾选这3笔案件（每笔卡片左上角出现勾选框）
4. 点击顶部「批量同意退款」按钮
5. 在弹窗中查看：
   - ✅ 汇总统计：总案件数=3，可处理数=3，退款金额汇总正确
   - ✅ 每笔案件显示当前状态=「客服仲裁」、版本号
   - ✅ 无可处理原因（全部可处理）
6. 填写备注：「批量处理客服仲裁案件」
7. 点击「确认提交」
8. 查看结果页：
   - ✅ 全部成功，成功数=3
   - ✅ 每笔案件新版本号递增
   - ✅ 批次号生成（格式：BATCH-YYYYMMDD-XXXX）

**验证**：
- 案件状态全部变为「退款完成」
- 版本号全部递增（如v3→v4）
- 点击「批量操作历史」可看到刚生成的批次记录

---

#### 链路2：混入旧版本冲突（部分失败）

**操作步骤**：
1. 登录客服账号，勾选3笔「客服仲裁」状态案件
2. **不要关闭当前页面**，新开浏览器窗口登录同一客服账号
3. 在新窗口中单独处理其中1笔案件（同意退款）
4. 回到原窗口，点击「批量同意退款」
5. 预览时仍显示3笔可处理
6. 提交后查看结果：
   - ✅ 总处理数=3，成功数=2，失败数=1
   - ✅ 失败的案件显示错误：「案件版本不匹配，请刷新后重试」
   - ✅ 失败案件的错误码：`VERSION_CONFLICT`
   - ✅ 成功的2笔案件状态变更正确

**验证**：
- 失败的案件状态仍为「客服仲裁」，版本号已被其他操作递增
- 成功的案件状态变为「退款完成」
- 批次详情中明确区分成功和失败的案件

---

#### 链路3：混入非客服仲裁状态（部分跳过）

**操作步骤**：
1. 确保案件列表中有：
   - 2笔「客服仲裁」状态案件
   - 1笔「待举证」状态案件
   - 1笔「退款完成」状态案件
2. 登录客服账号，同时勾选这4笔案件
3. 点击「批量同意退款」
4. 在预览弹窗中查看：
   - ✅ 总案件数=4，可处理数=2，不可处理数=2
   - ✅ 不可处理的案件显示红色原因：「仅「客服仲裁」状态可批量处理，当前状态：XXX」
   - ✅ 可处理的2笔显示状态=「客服仲裁」
   - ✅ 退款金额仅统计可处理的2笔
5. 提交后查看结果：
   - ✅ 总处理数=4，成功数=2，跳过数=2
   - ✅ 跳过的案件显示状态=skipped，原因与预览一致
   - ✅ 成功的2笔状态变更正确

**验证**：
- 只有「客服仲裁」状态的案件被处理
- 其他状态的案件保持原样
- 批次详情中skipped状态的案件有明确原因说明

---

#### 链路4：越权调用（商家/团长）

**操作步骤 - 商家验证**：
1. 登录商家账号 `merchant1` / `123456`
2. 进入案件列表：
   - ✅ 案件卡片**没有**勾选框
   - ✅ 顶部**没有**「批量同意退款」「批量驳回」「批量历史」按钮
   - ✅ 左侧菜单**没有**「批量操作历史」入口

**操作步骤 - 团长验证**：
1. 登录团长账号 `leader1` / `123456`
2. 进入案件列表：
   - ✅ 案件卡片**没有**勾选框
   - ✅ 顶部**没有**批量操作按钮
   - ✅ 左侧菜单**没有**「批量操作历史」入口

**API验证**：
```bash
# 使用商家token调用批量执行接口（会被拒绝）
curl -X POST http://localhost:3001/api/batch/execute \
  -H "Authorization: Bearer {商家token}" \
  -H "Content-Type: application/json" \
  -d '{"caseIds":[1,2],"action":"csRefund","remark":"越权测试","versions":{"1":3,"2":3}}'

# 预期返回：
# {"success":false,"error":{"code":"PERMISSION_DENIED","message":"无权限执行批量操作，仅客服可执行此操作"}}
```

---

#### 链路5：批量驳回功能

**操作步骤**：
1. 登录客服账号，勾选2笔「客服仲裁」状态案件
2. 点击「批量驳回」按钮
3. 预览确认可处理数=2
4. 填写备注：「证据不足，予以驳回」
5. 提交后查看结果：
   - ✅ 成功数=2
   - ✅ 每笔案件状态变为「驳回」
   - ✅ 版本号递增
   - ✅ 批次记录操作类型=「批量驳回」

---

#### 链路6：批量历史与CSV导出

**操作步骤**：
1. 登录客服账号，点击左侧「批量操作历史」
2. 查看批次列表：
   - ✅ 显示批次号、操作类型、操作人、操作时间、总数、成功数、失败数
   - ✅ 支持按日期范围筛选
   - ✅ 支持按操作类型筛选（批量退款/批量驳回）
3. 点击某批次的「详情」按钮：
   - ✅ 显示批次基本信息（批次号、操作人、备注、汇总统计）
   - ✅ 显示每笔案件的处理结果（成功/失败/跳过、原因、版本变化）
4. 点击「导出CSV」按钮：
   - ✅ 下载CSV文件
   - ✅ CSV包含：批次号、案件ID、订单号、处理状态、错误信息、原状态、新状态、原版本、新版本、退款金额
5. 验证CSV内容与页面显示一致

---

#### 链路7：数据持久化验证

**操作步骤**：
1. 完成一笔批量退款操作，记录批次号
2. 记录批次详情：成功数、失败数、每笔案件状态
3. **停止服务**（Ctrl+C）
4. **重新启动服务**（`npm run dev`）
5. 重新登录客服账号，验证：
   - ✅ 「批量操作历史」列表中批次记录仍在
   - ✅ 批次详情与重启前完全一致
   - ✅ 案件状态与重启前一致
   - ✅ 版本号与重启前一致
   - ✅ 重新导出CSV，内容与重启前导出的完全一致

---

### 自动化测试

#### 运行测试
```bash
npm run test:batch
```

#### 测试覆盖
测试脚本 `tests/batchOperation.test.ts` 覆盖10个测试用例：
1. ✅ 成功批量退款（全部可处理）
2. ✅ 混入旧版本冲突（部分失败）
3. ✅ 混入非客服仲裁状态（部分跳过）
4. ✅ 越权调用（商家/团长调用批量接口）
5. ✅ 批量驳回功能
6. ✅ 数据持久化验证（重启后数据一致）
7. ✅ 空批量处理验证
8. ✅ 无效操作类型验证
9. ✅ 缺少版本号信息验证
10. ✅ 并发修改版本冲突验证（双重校验）

---

## 七、撤销最近批次功能

### 功能概述
客服在批量操作历史里能对自己刚执行且仍未被后续处理的批次发起撤销，撤销前展示每笔案件原状态、新状态、当前版本和不可撤销原因；确认后逐笔用乐观锁回滚到批次前状态，已经被单独处理或再次批量处理过的案件只记失败明细，不影响其他可撤销项。团长、商家不能看到入口或调用接口，客服也不能撤销别人的批次。撤销动作要写入审计历史和批次详情，能导出CSV，重启后撤销记录、案件状态、版本号和导出内容保持一致。

### 核心特性
- **权限控制**：仅客服（cs角色）可见撤销入口和调用API，且只能撤销自己创建的批次
- **撤销预览**：提交前展示每笔案件的当前状态、当前版本、批次后状态、批次后版本、撤销目标状态、撤销目标版本，以及不可撤销原因
- **乐观锁回滚**：逐笔使用版本号校验，已被他人处理的案件不会被误改
- **部分失败处理**：返回详细的成功/失败/跳过明细，不影响其他正常案件
- **审计追踪**：每笔撤销操作生成审计记录，完整记录撤销人、备注、每笔结果
- **双重记录**：撤销信息同时写入批次表、批次明细表、撤销审计表、撤销明细表
- **CSV导出**：支持按批次导出包含撤销信息的操作结果，支持按撤销记录导出撤销明细
- **数据持久化**：重启后撤销记录、案件状态、版本号和导出内容完全一致

### 不可撤销原因说明
| 原因 | 说明 |
|------|------|
| 案件已被后续处理 | 案件版本号与批次处理后版本不一致，说明案件已被单独处理 |
| 案件状态已变更 | 案件状态与批次处理后状态不一致 |
| 案件已被其他批次处理 | 该案件已被其他后续批次成功处理 |
| 案件在批次中未成功处理 | 批次中该案件处理状态不是成功，无需撤销 |

---

### 验收步骤

#### 链路1：成功撤销批次（全部可撤销）

**前置条件**：
- 已完成一笔批量退款操作，所有案件处理成功
- 这些案件未被后续单独处理或再次批量处理

**操作步骤**：
1. 登录客服账号 `cs1` / `123456`
2. 点击左侧「批量操作历史」
3. 在列表中找到刚创建的批次，确认：
   - ✅ 批次号后没有「已撤销」标签
   - ✅ 操作列显示「撤销」按钮（橙色箭头图标）
4. 点击「撤销」按钮，打开撤销弹窗：
   - ✅ 弹窗标题显示「撤销批次」，显示批次号
   - ✅ 三步流程指示器显示当前在「预览」步骤
   - ✅ 顶部统计卡片显示：总案件数、可撤销数、不可撤销数、涉及金额、可撤销金额
   - ✅ 每笔案件显示：当前状态、当前版本、批次后状态、批次后版本、撤销目标状态、撤销目标版本
   - ✅ 所有案件的「撤销目标状态」为「客服仲裁」
   - ✅ 所有案件的「撤销目标版本」为批次处理前的版本号（如v3）
   - ✅ 所有案件显示绿色可撤销图标，没有红色不可撤销原因
5. 在「撤销备注」中填写：「操作有误，予以撤销」
6. 点击「确认撤销」按钮，进入确认页面：
   - ✅ 显示橙色警告图标和确认提示
   - ✅ 显示批次号、可撤销案件数、涉及金额
7. 点击「确认提交撤销」按钮，执行撤销：
   - ✅ 撤销完成后显示结果页面
   - ✅ 统计卡片显示：总计、撤销成功、撤销失败、跳过、涉及金额、成功金额
   - ✅ 所有案件显示「撤销成功」状态，版本号递增到v5
   - ✅ 撤销记录ID显示在弹窗标题下方

**验证**：
1. 关闭弹窗后，批次列表中该批次号后出现「已撤销」橙色标签
2. 点击进入批次详情：
   - ✅ 标题显示「已撤销」标签
   - ✅ 显示撤销信息卡片：撤销状态=已撤销、撤销时间、撤销人=王客服、撤销备注=「操作有误，予以撤销」
   - ✅ 表格新增「撤销状态」列，所有案件显示「撤销成功」
   - ✅ 错误信息列显示「-」
3. 进入案件列表，查看原案件：
   - ✅ 状态已从「退款完成」回滚为「客服仲裁」
   - ✅ 版本号从v4递增到v5（撤销操作新增版本记录）
4. 进入案件详情页，查看版本历史：
   - ✅ 最新版本记录的操作类型为「同意退款」
   - ✅ 最新版本记录的目标状态为「客服仲裁」
   - ✅ 最新版本记录的备注包含「撤销批次」字样
5. 点击批次详情的「导出CSV」按钮：
   - ✅ 下载CSV文件
   - ✅ CSV包含「是否已撤销」「撤销时间」「撤销人」「撤销备注」列
   - ✅ 「是否已撤销」列显示「是」
   - ✅ 「撤销备注」列显示「操作有误，予以撤销」

---

#### 链路2：撤销预览-混入版本冲突（部分不可撤销）

**前置条件**：
- 已完成一笔批量退款操作，包含3笔案件
- 其中1笔案件已被单独处理（状态或版本已变更）

**操作步骤**：
1. 登录客服账号 `cs1`
2. 在批量操作历史中找到该批次，点击「撤销」按钮
3. 在撤销预览页面查看：
   - ✅ 总案件数=3，可撤销数=2，不可撤销数=1
   - ✅ 顶部显示黄色警告：「存在1笔不可撤销的案件」
   - ✅ 2笔可撤销案件显示绿色图标
   - ✅ 1笔不可撤销案件显示红色背景和红色图标
   - ✅ 不可撤销案件显示红色错误原因：「案件 XXX 已被后续处理（当前版本v5，批次处理后版本v4），无法撤销」
4. 查看统计卡片：
   - ✅ 可撤销金额只统计2笔可撤销案件的金额
   - ✅ 不可撤销金额不统计

**验证**：
- 不可撤销的案件在列表中高亮显示，原因清晰明确
- 即使有不可撤销的案件，只要有可撤销案件，「确认撤销」按钮仍可用

---

#### 链路3：撤销执行-混入版本冲突（部分失败）

**前置条件**：
- 已完成一笔批量退款操作，包含3笔案件，全部可撤销

**操作步骤**：
1. 打开撤销弹窗，进入预览页面
2. **不要关闭弹窗**，新开浏览器窗口登录同一客服账号
3. 在新窗口中单独处理其中1笔案件（如同意退款或驳回）
4. 回到原弹窗，填写撤销备注，点击「确认撤销」→「确认提交撤销」
5. 查看撤销结果：
   - ✅ 总处理数=3，成功数=2，失败数=1，跳过数=0
   - ✅ 成功的2笔显示「撤销成功」，版本号递增
   - ✅ 失败的1笔显示「撤销失败」，红色错误信息：「案件 XXX 版本不匹配，当前版本v5，预期版本v4，请刷新后重试」
   - ✅ 失败案件的错误码为 `VERSION_CONFLICT`

**验证**：
1. 批次详情中：
   - ✅ 批次整体标记为「已撤销」（因为有成功撤销的案件）
   - ✅ 撤销状态列区分显示「撤销成功」和「撤销失败」
   - ✅ 失败案件的错误信息列显示具体原因
2. 案件状态：
   - ✅ 成功撤销的2笔案件状态回滚为「客服仲裁」
   - ✅ 失败的1笔案件状态保持为「退款完成」（被其他操作变更后的状态）

---

#### 链路4：越权调用（商家/团长）

**操作步骤 - 商家验证**：
1. 登录商家账号 `merchant1` / `123456`
2. 查看左侧菜单：
   - ✅ 没有「批量操作历史」入口
3. 直接访问 `/batch` 路径：
   - ✅ 被重定向到首页或显示无权限
4. 查看案件列表：
   - ✅ 案件卡片没有勾选框
   - ✅ 顶部没有批量操作按钮

**操作步骤 - 团长验证**：
1. 登录团长账号 `leader1` / `123456`
2. 重复上述商家验证步骤：
   - ✅ 同样没有「批量操作历史」入口和撤销按钮

**API验证**：
```bash
# 使用商家token调用撤销预览接口（会被拒绝）
curl -X POST http://localhost:3001/api/batch/revoke/preview \
  -H "Authorization: Bearer {商家token}" \
  -H "Content-Type: application/json" \
  -d '{"batchId":1}'

# 预期返回：
# {"success":false,"error":{"code":"PERMISSION_DENIED","message":"无权限撤销批次，仅客服可执行此操作"}}
```

---

#### 链路5：撤销他人批次（跨客服）

**前置条件**：
- 客服A（cs1）创建了一个批次
- 客服B（假设存在，或模拟其他客服ID）尝试撤销

**操作步骤**：
1. 使用另一个客服账号（或通过API模拟）尝试撤销客服A创建的批次
2. 点击撤销按钮或调用API：
   - ✅ 预览接口返回错误：`{"success":false,"error":{"code":"BATCH_NOT_OWNED","message":"不能撤销他人执行的批次"}}`
   - ✅ 执行接口返回同样的错误

**前端验证**：
- 在批量操作历史列表中，其他客服创建的批次操作列不显示「撤销」按钮
- 只有当前登录客服自己创建的批次才显示撤销按钮

---

#### 链路6：撤销已撤销的批次

**前置条件**：
- 已成功撤销一个批次

**操作步骤**：
1. 在批量操作历史中找到已撤销的批次：
   - ✅ 批次号后显示「已撤销」标签
   - ✅ 操作列不显示「撤销」按钮
2. 进入批次详情：
   - ✅ 标题显示「已撤销」标签
   - ✅ 不显示「撤销批次」按钮
3. 通过API尝试再次撤销：
   - ✅ 预览接口返回错误：`{"success":false,"error":{"code":"BATCH_ALREADY_REVOKED","message":"该批次已被撤销"}}`
   - ✅ 执行接口返回同样的错误

---

#### 链路7：撤销-案件已被其他批次处理

**前置条件**：
- 批次1处理了案件A，状态变为「退款完成」
- 手动将案件A状态改回「客服仲裁」（模拟单独调整）
- 批次2再次处理案件A，状态变为「驳回」

**操作步骤**：
1. 尝试撤销批次1：
   - ✅ 预览页面显示案件A不可撤销
   - ✅ 不可撤销原因：「案件 XXX 已被其他批次处理，无法撤销」
   - ✅ `canRevokeBatch = false`，「确认撤销」按钮禁用
   - ✅ 显示红色提示：「无法撤销该批次 - 该批次没有可撤销的案件」

---

#### 链路8：数据持久化验证-撤销后重启一致性

**前置条件**：
- 已完成一笔撤销操作，包含2笔成功撤销，1笔失败

**操作步骤**：
1. 记录撤销前的状态：
   - 批次号、撤销记录ID
   - 每笔案件的状态、版本号
   - 撤销成功数、失败数、跳过数
   - 导出批次CSV和撤销记录CSV的内容
2. **停止服务**（Ctrl+C）
3. **重新启动服务**（`npm run dev`）
4. 重新登录客服账号，验证：
   - ✅ 「批量操作历史」列表中批次记录仍在，显示「已撤销」标签
   - ✅ 批次详情与重启前完全一致（撤销信息、统计数据、明细）
   - ✅ 案件状态与重启前一致（成功撤销的回滚为「客服仲裁」，失败的保持不变）
   - ✅ 版本号与重启前一致（成功撤销的版本号递增，失败的保持不变）
   - ✅ 撤销详情中的统计数据与重启前一致
   - ✅ 重新导出批次CSV，内容与重启前导出的完全一致（包含撤销信息）
   - ✅ 重新导出撤销记录CSV，内容与重启前导出的完全一致
   - ✅ 版本历史记录完整，包含撤销操作的版本记录

---

#### 链路9：撤销CSV导出内容验证

**操作步骤**：
1. 完成一笔撤销操作
2. 在批次详情页点击「导出CSV」：
   - ✅ 下载的CSV文件名包含批次ID和时间戳
   - ✅ CSV包含21列：批次号、操作类型、操作人、操作备注、案件ID、订单号、退款金额、原状态、原版本号、处理结果、新状态、新版本号、错误码、错误信息、处理时间、是否已撤销、撤销时间、撤销人、撤销备注
   - ✅ 「是否已撤销」列显示「是」
   - ✅ 「撤销备注」列显示撤销时填写的备注
3. 通过撤销记录导出接口（或在撤销详情页）导出撤销记录CSV：
   - ✅ CSV包含18列：撤销记录ID、批次号、操作类型、操作人、操作备注、案件ID、订单号、退款金额、撤销前状态、撤销前版本号、撤销目标状态、撤销目标版本号、处理结果、撤销后状态、撤销后版本号、错误码、错误信息、处理时间
   - ✅ 「处理结果」列显示「撤销成功」「撤销失败」或「已跳过」
   - ✅ 失败案件的「错误码」和「错误信息」列显示具体内容
   - ✅ CSV内容与页面显示完全一致

---

#### 链路10：撤销-没有成功案件的批次

**前置条件**：
- 创建一个批次，其中所有案件都处理失败或被跳过（successCount = 0）

**操作步骤**：
1. 在批量操作历史中找到该批次：
   - ✅ 操作列不显示「撤销」按钮
2. 进入批次详情：
   - ✅ 不显示「撤销批次」按钮
3. 通过API尝试撤销：
   - ✅ 返回错误：`{"success":false,"error":{"code":"BATCH_NOT_REVOCABLE","message":"该批次没有成功处理的案件，无需撤销"}}`

---

### 自动化测试

#### 运行所有测试
```bash
npm run test:all
```

#### 分别运行测试
```bash
# 运行后端批量操作测试（包含撤销功能）
npm run test:batch

# 运行前端逻辑测试
npm run test:frontend
```

#### 测试覆盖

**后端测试 - `tests/batchOperation.test.ts`**（20个用例）：
1-10. 原有批量操作测试用例
11. ✅ 成功撤销批次（全部可撤销）
12. ✅ 撤销预览-混入版本冲突（部分不可撤销）
13. ✅ 撤销执行-混入版本冲突（部分失败）
14. ✅ 越权撤销（商家/团长调用撤销接口）
15. ✅ 撤销他人批次（跨客服）
16. ✅ 撤销已撤销的批次
17. ✅ 撤销-案件已被其他批次处理
18. ✅ 数据持久化验证-撤销后重启一致性
19. ✅ 撤销-没有成功案件的批次
20. ✅ 撤销CSV导出内容验证

**前端测试 - `tests/batchRevokeFrontend.test.ts`**（8个用例）：
1. ✅ 常量标签验证
2. ✅ canRevokeBatch 逻辑验证（权限、归属、状态等）
3. ✅ 撤销预览数据处理验证
4. ✅ 错误码验证
5. ✅ 撤销结果统计验证
6. ✅ 状态标签映射验证
7. ✅ 不可撤销原因分类验证
8. ✅ 撤销金额计算验证

---

## 八、售后仲裁规则配置与自动分派功能

### 功能概述
客服可以维护按售后类型、责任方、退款金额区间和商家维度匹配的仲裁规则，规则能设置默认处理建议、优先级、启停状态和备注。团长提交案件或商家响应后，系统自动给出命中的规则、建议动作和分派客服，案件详情里能看到命中原因。规则列表支持导入/导出CSV，导入时处理重复优先级、无效区间和并发版本冲突。只允许客服管理规则，团长和商家只能看到案件上的建议结果。规则变更、自动命中、人工覆盖都要写审计日志，SQLite重启后配置、命中记录、分派结果和导出内容保持一致。

### 核心特性
- **多维度匹配**：支持按售后类型、责任方、退款金额区间、商家ID匹配
- **优先级排序**：数字越小优先级越高，确保高优先级规则先匹配
- **默认处理建议**：支持同意退款、驳回、人工审核三种建议
- **自动分派**：规则可指定分派客服，匹配后自动分派
- **启停状态**：规则可随时启用/禁用，禁用规则不参与匹配
- **CSV导入导出**：支持批量规则管理，导出格式可直接编辑后重新导入
- **冲突处理**：导入时自动检测并调整重复优先级，检测无效金额区间
- **并发控制**：使用版本号防止并发修改冲突
- **权限控制**：仅客服可管理规则，团长和商家仅能查看案件上的建议结果
- **审计日志**：规则创建、更新、删除、启用、禁用、命中、人工覆盖、导入、导出全记录
- **人工覆盖**：客服可人工覆盖系统建议，覆盖操作记录审计日志
- **数据持久化**：重启后规则配置、命中记录、分派结果、导出内容完全一致

### 规则匹配优先级说明
1. 系统按优先级从小到大遍历所有启用的规则
2. 每个规则可配置多个条件（售后类型、责任方、金额区间、商家）
3. 条件为空表示匹配任意值（通用规则）
4. 所有非空条件都匹配时，规则命中
5. 返回第一个命中的规则（优先级最高）

---

### 验收步骤

#### 链路1：客服创建规则成功

**操作步骤**：
1. 登录客服账号 `cs1` / `123456`
2. 点击左侧「规则配置」菜单
3. 点击「新建规则」按钮
4. 填写规则表单：
   - 优先级：`1`
   - 售后类型：`破损`
   - 责任方：`商家`
   - 金额下限：`0`
   - 金额上限：`100`
   - 商家：`张商家`
   - 建议动作：`同意退款`
   - 分派客服：`王客服`
   - 备注：`100元以下破损案件自动同意退款`
   - 启用状态：`启用`
5. 点击「确定」提交
6. 查看规则列表：
   - ✅ 新规则出现在列表中
   - ✅ 优先级、售后类型、责任方等信息正确显示
   - ✅ 状态显示「启用」

**验证**：
- 规则列表中正确显示新创建的规则
- 点击规则的「编辑」按钮，表单中数据与创建时一致
- 审计日志中记录了规则创建操作

---

#### 链路2：非客服角色创建规则被拒绝（权限控制）

**操作步骤 - 商家验证**：
1. 登录商家账号 `merchant1` / `123456`
2. 查看左侧菜单：
   - ✅ 没有「规则配置」入口
3. 直接访问 `/rules` 路径：
   - ✅ 被重定向到首页或显示无权限

**操作步骤 - 团长验证**：
1. 登录团长账号 `leader1` / `123456`
2. 重复上述商家验证步骤：
   - ✅ 同样没有「规则配置」入口

**API验证**：
```bash
# 使用商家token调用创建规则接口（会被拒绝）
curl -X POST http://localhost:3001/api/rules \
  -H "Authorization: Bearer {商家token}" \
  -H "Content-Type: application/json" \
  -d '{"caseType":"damaged","responsibleParty":"merchant","priority":1,"suggestedAction":"csRefund"}'

# 预期返回：
# {"success":false,"error":{"code":"INVALID_RULE","message":"只有客服角色可以创建仲裁规则"}}
```

---

#### 链路3：重复优先级创建失败

**操作步骤**：
1. 登录客服账号
2. 先创建一条优先级为1的规则
3. 再次创建规则，优先级也填写1
4. 点击「确定」：
   - ✅ 系统提示错误：「优先级 1 已存在，请使用其他优先级」
   - ✅ 错误码：`DUPLICATE_PRIORITY`
5. 修改优先级为2，再次提交：
   - ✅ 创建成功

**验证**：
- 优先级具有唯一性约束
- 相同优先级无法创建多条规则

---

#### 链路4：无效金额区间创建失败

**操作步骤**：
1. 登录客服账号
2. 创建规则，填写：
   - 金额下限：`100`
   - 金额上限：`50`
3. 点击「确定」：
   - ✅ 系统提示错误：「金额上限必须大于等于金额下限」
   - ✅ 错误码：`INVALID_AMOUNT_RANGE`
4. 修改金额上限为200，再次提交：
   - ✅ 创建成功

**验证**：
- 金额区间必须有效（下限 <= 上限）
- 负金额区间被拒绝

---

#### 链路5：规则自动匹配-团长提交案件后

**前置条件**：
- 已创建一条规则：破损、商家、0-100元、建议同意退款、分派王客服

**操作步骤**：
1. 登录团长账号 `leader1`
2. 创建一个售后申请：
   - 售后类型：`破损`
   - 责任方：`商家`
   - 退款金额：`50`
   - 商家：`张商家`
3. 提交凭证，状态流转为「商家处理」
4. 登录商家账号 `merchant1`
5. 响应该案件，状态流转为「客服仲裁」
6. 登录客服账号 `cs1`
7. 进入案件详情页：
   - ✅ 显示「规则命中信息」卡片
   - ✅ 显示命中的规则：优先级1，建议动作「同意退款」
   - ✅ 显示命中原因：「售后类型匹配: 破损; 责任方匹配: 商家; 退款金额匹配: 50元 在区间 0 - 100 内; 商家匹配: 张商家」
   - ✅ 显示分派客服：「王客服」
   - ✅ 显示「人工覆盖」按钮

**验证**：
- 商家响应后系统自动匹配规则
- 案件详情正确显示命中规则、建议动作、命中原因、分派客服
- 命中记录和审计日志已创建

---

#### 链路6：人工覆盖规则建议

**前置条件**：
- 案件已命中规则，建议动作是「同意退款」

**操作步骤**：
1. 登录客服账号
2. 进入案件详情页
3. 在「规则命中信息」卡片中点击「人工覆盖」按钮
4. 在弹窗中填写：
   - 选择新的建议动作：`驳回`
   - 覆盖备注：「客户多次恶意申请，予以驳回」
5. 点击「确认覆盖」：
   - ✅ 规则命中信息卡片更新
   - ✅ 显示「已覆盖」标签
   - ✅ 显示覆盖后的建议动作：`驳回`
   - ✅ 显示覆盖备注：「客户多次恶意申请，予以驳回」
   - ✅ 显示覆盖人：`王客服`
   - ✅ 显示覆盖时间

**验证**：
- 覆盖后规则命中信息正确更新
- 审计日志中记录了人工覆盖操作
- 「人工覆盖」按钮变为不可用（已覆盖）

---

#### 链路7：非客服角色无法覆盖规则

**操作步骤 - 商家验证**：
1. 登录商家账号
2. 进入已命中规则的案件详情页：
   - ✅ 显示规则命中信息卡片（只读）
   - ✅ 没有「人工覆盖」按钮
   - ✅ 只能查看建议结果，不能修改

**操作步骤 - 团长验证**：
1. 登录团长账号
2. 进入已命中规则的案件详情页：
   - ✅ 同样显示只读的规则命中信息
   - ✅ 没有「人工覆盖」按钮

---

#### 链路8：规则启停功能

**操作步骤**：
1. 登录客服账号
2. 进入规则配置页面
3. 在规则列表中找到一条启用的规则
4. 点击「禁用」按钮：
   - ✅ 规则状态变为「禁用」
   - ✅ 按钮变为「启用」
5. 创建一个应该命中该规则的案件：
   - ✅ 案件详情中没有命中该禁用的规则
   - ✅ 系统提示「未匹配到规则」
6. 回到规则配置页面，点击「启用」按钮：
   - ✅ 规则状态变为「启用」
7. 重新创建相同条件的案件：
   - ✅ 案件成功命中该规则

**验证**：
- 禁用的规则不参与匹配
- 启用后恢复匹配
- 启停操作记录审计日志

---

#### 链路9：规则更新-版本冲突检测

**操作步骤**：
1. 登录客服账号
2. 打开两个浏览器窗口，都进入规则配置页面
3. 在两个窗口中都点击同一条规则的「编辑」按钮
4. 在窗口A中修改规则备注，保存成功
5. 在窗口B中（仍显示旧版本）修改备注并保存：
   - ✅ 系统提示错误：「规则已被修改，请刷新后重试」
   - ✅ 错误码：`VERSION_CONFLICT`

**验证**：
- 并发修改时使用版本号进行乐观锁控制
- 旧版本修改被拒绝

---

#### 链路10：CSV导出规则

**操作步骤**：
1. 登录客服账号
2. 进入规则配置页面
3. 确保至少有2-3条规则
4. 点击「导出CSV」按钮：
   - ✅ 下载CSV文件
   - ✅ CSV包含列：优先级、售后类型、责任方、金额下限、金额上限、商家ID、建议动作、分派客服ID、备注、启用状态
   - ✅ 数据内容与页面显示一致
5. 打开CSV文件验证：
   - ✅ 中文标签正确（破损、商家、同意退款等）
   - ✅ 空值显示为空字符串
   - ✅ 启用状态显示为`true`或`false`

---

#### 链路11：CSV导入-成功导入多条规则

**操作步骤**：
1. 登录客服账号
2. 进入规则配置页面
3. 点击「导入CSV」按钮
4. 选择准备好的CSV文件（3条有效规则，优先级分别为10、20、30）
5. 点击「确定」导入：
   - ✅ 显示导入结果弹窗
   - ✅ 成功数=3，失败数=0
   - ✅ 规则列表中新增3条规则

**CSV文件格式示例**：
```csv
优先级,售后类型,责任方,金额下限,金额上限,商家ID,建议动作,分派客服ID,备注,启用状态
10,damaged,merchant,0,100,,csRefund,,导入测试规则1,true
20,outOfStock,platform,50,200,,csReject,,导入测试规则2,true
30,wrongDelivery,logistics,100,500,,review,,导入测试规则3,true
```

---

#### 链路12：CSV导入-重复优先级自动调整

**操作步骤**：
1. 登录客服账号
2. 先创建一条优先级为1的规则
3. 准备CSV文件，其中一条规则优先级也为1
4. 导入该CSV文件：
   - ✅ 导入成功
   - ✅ 导入的规则优先级自动调整为下一个可用的优先级（如2）
   - ✅ 导入结果中显示优先级调整信息
5. 查看规则列表：
   - ✅ 原有规则优先级保持为1
   - ✅ 导入的规则优先级为2

**验证**：
- 导入时自动检测重复优先级
- 自动调整到下一个可用优先级，不覆盖原有规则
- 调整信息记录在导入结果中

---

#### 链路13：CSV导入-无效区间检测

**操作步骤**：
1. 准备CSV文件，包含2条规则：
   - 规则1：金额下限100，金额上限50（无效）
   - 规则2：金额下限0，金额上限100（有效）
2. 导入该CSV文件：
   - ✅ 导入结果显示：成功数=1，失败数=1
   - ✅ 失败的规则显示错误原因：「金额上限必须大于等于金额下限」
   - ✅ 有效的规则成功导入

**验证**：
- 导入时逐行校验数据有效性
- 无效数据被拒绝，不影响有效数据导入
- 失败原因清晰明确

---

#### 链路14：CSV导入导出一致性

**操作步骤**：
1. 登录客服账号
2. 创建2-3条规则（包含不同的售后类型、责任方、建议动作）
3. 点击「导出CSV」，保存文件A
4. 删除所有规则
5. 点击「导入CSV」，选择文件A
6. 验证导入成功，规则数相同
7. 再次点击「导出CSV」，保存文件B
8. 比较文件A和文件B的数据内容：
   - ✅ 规则数据完全一致（除了自动调整的优先级）
   - ✅ 列顺序和格式完全一致

---

#### 链路15：数据持久化验证-重启后配置一致

**操作步骤**：
1. 登录客服账号
2. 创建一条规则，记录规则ID、优先级、备注等信息
3. 创建一个案件，使其命中该规则
4. 人工覆盖该规则建议
5. 导出规则CSV，保存文件内容
6. 记录：
   - 规则配置信息
   - 案件命中记录（命中原因、建议动作）
   - 覆盖记录（覆盖备注、覆盖人）
   - 审计日志数量
   - CSV导出内容
7. **停止服务**（Ctrl+C）
8. **重新启动服务**（`npm run dev`）
9. 重新登录客服账号，验证：
   - ✅ 规则配置页面中规则仍在，配置信息完全一致
   - ✅ 案件详情中命中记录和覆盖信息与重启前一致
   - ✅ 审计日志数量和内容与重启前一致
   - ✅ 重新导出CSV，内容与重启前导出的完全一致
   - ✅ 新建相同条件的案件仍能正确命中规则

---

#### 链路16：审计日志-规则变更、命中、人工覆盖有记录

**操作步骤**：
1. 登录客服账号
2. 创建一条新规则
3. 查看审计日志（通过API或数据库）：
   - ✅ 记录操作类型：`rule_create`
   - ✅ 记录操作人：王客服
   - ✅ 记录规则ID和变更内容
4. 创建一个案件使其命中该规则：
   - ✅ 记录操作类型：`rule_hit`
   - ✅ 记录命中原因和规则信息
5. 人工覆盖规则建议：
   - ✅ 记录操作类型：`rule_override`
   - ✅ 记录覆盖备注和原规则信息
6. 修改规则：
   - ✅ 记录操作类型：`rule_update`
   - ✅ 记录变更前后的差异
7. 禁用规则：
   - ✅ 记录操作类型：`rule_status_change`
   - ✅ 记录状态从`启用`变为`禁用`
8. 删除规则：
   - ✅ 记录操作类型：`rule_delete`
   - ✅ 记录被删除的规则信息

---

#### 链路17：规则列表筛选功能

**操作步骤**：
1. 登录客服账号
2. 进入规则配置页面
3. 创建多条不同类型的规则（不同售后类型、责任方、状态）
4. 使用筛选功能：
   - 按售后类型筛选「破损」：
     - ✅ 只显示售后类型为破损的规则
   - 按责任方筛选「商家」：
     - ✅ 只显示责任方为商家的规则
   - 按状态筛选「禁用」：
     - ✅ 只显示禁用的规则
   - 按优先级排序：
     - ✅ 规则按优先级从小到大排序

---

#### 链路18：删除规则功能

**操作步骤**：
1. 登录客服账号
2. 进入规则配置页面
3. 创建一条测试规则
4. 点击该规则的「删除」按钮
5. 确认删除：
   - ✅ 规则从列表中消失
   - ✅ 审计日志中记录删除操作
6. 创建一个应该命中该规则的案件：
   - ✅ 案件不再命中已删除的规则
7. 查看之前命中过该规则的案件详情：
   - ✅ 仍然显示历史命中记录（规则名称显示为「已删除」或保留原名称）

---

#### 链路19：通用规则匹配（空条件）

**操作步骤**：
1. 登录客服账号
2. 创建一条规则：
   - 优先级：`999`
   - 售后类型：`不限制`（空）
   - 责任方：`不限制`（空）
   - 金额下限：`0`
   - 金额上限：`999999`
   - 商家：`不限制`（空）
   - 建议动作：`人工审核`
   - 备注：`通用规则，兜底使用`
3. 创建一个不匹配任何其他规则的案件：
   - ✅ 案件命中这条通用规则
   - ✅ 命中原因显示：「售后类型: 任意; 责任方: 任意; 退款金额匹配: XXX元 在区间 0 - 999999 内; 商家: 任意」

**验证**：
- 空条件表示匹配任意值
- 通用规则可作为兜底规则使用

---

### 自动化测试

#### 运行规则测试
```bash
npm run test:rules
```

#### 运行所有测试
```bash
npm run test:all
```

#### 测试覆盖
测试脚本 `tests/ruleOperation.test.ts` 覆盖27个测试用例：

1. ✅ 客服创建规则成功
2. ✅ 非客服角色创建规则被拒绝（权限控制）
3. ✅ 重复优先级创建失败
4. ✅ 无效金额区间创建失败
5. ✅ 更新规则-版本冲突检测
6. ✅ 规则启停功能
7. ✅ 规则匹配-按优先级匹配
8. ✅ 规则匹配-商家维度精确匹配
9. ✅ 规则匹配-禁用规则不参与匹配
10. ✅ 人工覆盖规则建议
11. ✅ 非客服角色无法覆盖规则
12. ✅ 审计日志-规则创建有记录
13. ✅ 审计日志-规则命中有记录
14. ✅ 审计日志-人工覆盖有记录
15. ✅ CSV导出规则
16. ✅ CSV导入-成功导入多条规则
17. ✅ CSV导入-重复优先级自动调整
18. ✅ CSV导入-无效区间检测
19. ✅ CSV导入导出一致性
20. ✅ 数据持久化验证-重启后配置一致
21. ✅ 规则列表筛选功能
22. ✅ 删除规则功能
23. ✅ 获取客服列表
24. ✅ 规则匹配-空条件通用匹配
25. ✅ 审计日志-导入导出有记录
26. ✅ 案件详情-团长和商家只能查看命中结果
27. ✅ 并发修改-版本冲突双重验证

---

## 九、失败链路验证

### 失败链路1：商家越权裁决

**预期错误**：`无权限执行此操作` / `PERMISSION_DENIED`

**复现步骤**：
1. 登录商家账号 `merchant1`
2. 找到一个状态为「客服仲裁」的案件
3. 尝试直接点击「同意退款」或「驳回」
4. 系统返回权限错误

**API验证**：
```bash
# 使用商家token调用客服裁决接口（会被拒绝）
curl -X POST http://localhost:3001/api/cases/{caseId}/action \
  -H "Authorization: Bearer {商家token}" \
  -H "Content-Type: application/json" \
  -d '{"action":"csRefund","version":2,"remark":"越权操作"}'
```

### 失败链路2：缺少凭证提交

**预期错误**：`请提供凭证URL` / `MISSING_EVIDENCE`

**复现步骤**：
1. 登录团长账号 `leader1`
2. 创建一个新的售后申请（状态为「待举证」）
3. 进入详情页，在「提交凭证」表单中**不填凭证URL**
4. 点击提交，系统返回错误

**API验证**：
```bash
# 提交凭证但缺少evidenceUrl
curl -X POST http://localhost:3001/api/cases/{caseId}/action \
  -H "Authorization: Bearer {团长token}" \
  -H "Content-Type: application/json" \
  -d '{"action":"submitEvidence","version":1,"remark":"缺少凭证"}'
```

### 失败链路3：旧版本重复处理

**预期错误**：`案件版本不匹配，请刷新后重试` / `VERSION_CONFLICT`

**复现步骤**：
1. 打开两个浏览器窗口，都登录团长账号
2. 进入同一个案件的详情页（版本号为v1）
3. 在窗口A中提交凭证，状态流转成功（版本变为v2）
4. 在窗口B中（仍显示v1）尝试提交凭证
5. 系统返回版本冲突错误

**API验证**：
```bash
# 第一次提交成功（版本1→2）
curl -X POST http://localhost:3001/api/cases/{caseId}/action \
  -H "Authorization: Bearer {团长token}" \
  -H "Content-Type: application/json" \
  -d '{"action":"submitEvidence","version":1,"evidenceUrl":"https://example.com/1.jpg","remark":"第一次提交"}'

# 第二次使用旧版本号提交（失败）
curl -X POST http://localhost:3001/api/cases/{caseId}/action \
  -H "Authorization: Bearer {团长token}" \
  -H "Content-Type: application/json" \
  -d '{"action":"submitEvidence","version":1,"evidenceUrl":"https://example.com/2.jpg","remark":"重复提交"}'
```

---

## 七、数据持久化验证

### 验证步骤
1. 完成一笔完整的退款流程
2. 记录案件编号、当前状态、版本号
3. **停止服务**（Ctrl+C）
4. **重新启动服务**
5. 重新登录，验证：
   - ✅ 案件列表顺序不变
   - ✅ 案件状态正确
   - ✅ 版本历史完整
   - ✅ 退款导出内容与重启前一致

### 数据库文件
数据库文件位于 `data/database.sqlite`，删除该文件可重置所有数据。

---

## 八、API接口说明

### 认证
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/login` | 登录获取token |
| POST | `/api/auth/logout` | 登出 |

### 案件管理
| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/cases` | 获取案件列表（支持筛选） | 所有登录用户 |
| GET | `/api/cases/:id` | 获取案件详情 | 所有登录用户 |
| POST | `/api/cases` | 创建售后申请 | 团长 |
| POST | `/api/cases/:id/action` | 执行案件操作 | 对应角色 |
| GET | `/api/cases/merchants` | 获取商家列表 | 团长 |

### 批量操作
| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/api/batch/preview` | 批量操作预览 | 客服 |
| POST | `/api/batch/execute` | 批量操作执行 | 客服 |
| GET | `/api/batch` | 获取批量操作列表 | 客服 |
| GET | `/api/batch/:batchIdOrNo` | 获取批量操作详情 | 客服 |
| GET | `/api/batch/:batchId/export` | 导出批次操作结果CSV | 客服 |
| POST | `/api/batch/revoke/preview` | 撤销批次预览 | 客服 |
| POST | `/api/batch/revoke/execute` | 撤销批次执行 | 客服 |
| GET | `/api/batch/revoke/:revokeId/export` | 导出撤销记录CSV | 客服 |

### 导出
| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/export/refunds` | 导出退款清单CSV | 客服 |

---

## 九、样例数据说明

系统启动时自动初始化以下演示数据：

### 用户表（users）
- 3个演示用户（团长、商家、客服）
- 密码使用bcrypt加密存储

### 案件表（cases）- 可选预置
可通过访问系统后手动创建，初始数据库中无预置案件。

### 版本历史表（case_versions）
记录案件每一步状态变更，包含：
- 案件ID
- 版本号（从1开始递增）
- 变更前状态
- 变更后状态
- 操作人ID、姓名、角色
- 操作备注
- 操作时间

### 凭证表（evidences）
记录每个案件提交的凭证：
- 案件ID
- 版本号
- 上传人ID
- 凭证类型（图片/视频/其他）
- 凭证URL
- 备注
- 上传时间

---

## 十、技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端 | React | 18 |
| 前端 | TypeScript | 5 |
| 前端 | Vite | 6 |
| 前端 | TailwindCSS | 3 |
| 前端 | Zustand | 5 |
| 前端 | React Router | 7 |
| 前端 | Lucide React | 0.511 |
| 后端 | Express | 4 |
| 后端 | TypeScript | 5 |
| 后端 | better-sqlite3 | 12 |
| 后端 | jsonwebtoken | 9 |
| 后端 | bcryptjs | 3 |
| 数据库 | SQLite | 3 |

---

## 十五、常见问题

### Q: 启动后数据库文件在哪？
A: 在项目根目录的 `data/database.sqlite`

### Q: 如何重置数据？
A: 删除 `data/database.sqlite` 文件，重启服务即可重新初始化

### Q: 前端无法访问后端？
A: 确认后端服务已启动（端口3001），检查 vite.config.ts 中的代理配置

### Q: Token过期了怎么办？
A: 重新登录即可，Token有效期24小时

---

**启动命令总结**：
```bash
npm install && npm run dev
```
